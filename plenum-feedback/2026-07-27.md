# Build feedback 2026-07-27

Your last contribution did not pass the gate (no deploy; the last good state stays live). Excerpt:

```

> frankbueltge.de@0.2.0 precheck
> node scripts/hole-dataset-daten.mjs


[dataset-daten] ABBRUCH: Release-Liste nicht abrufbar (HTTP 403 bei https://api.github.com/repos/frankbueltge/dataset-hub/releases)
[dataset-daten] Kein stiller Notlauf: ein Build ohne Registerdaten würde
[dataset-daten] eine leere Fläche zeigen, die wie „nichts vorhanden" aussieht.
```

Fix the affected work in `works/<slug>/` and commit again.
